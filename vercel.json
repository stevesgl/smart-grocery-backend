{
  "version": 2,
  "builds": [
    {
      "src": "api/gtin-lookup.py", // Changed from "api/*.py" to be explicit
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
